# ============================================
# Supabase 資料庫連線設定
# ============================================
# 從 Supabase Dashboard > Settings > Database 取得
# Connection string 格式: postgresql://postgres:[YOUR-PASSWORD]@[PROJECT-REF].supabase.co:5432/postgres
DATABASE_URL="postgresql://postgres:YOUR_PASSWORD@YOUR_PROJECT_REF.supabase.co:5432/postgres"

# Supabase Project URL (用於 REST API 和 Auth)
# 從 Supabase Dashboard > Settings > API 取得
SUPABASE_URL="https://YOUR_PROJECT_REF.supabase.co"

# Supabase Anon/Public Key (用於客戶端)
# 從 Supabase Dashboard > Settings > API > Project API keys 取得
SUPABASE_ANON_KEY="your-anon-key-here"

# Supabase Service Role Key (用於服務端，具有完整權限，請勿在客戶端使用)
# 從 Supabase Dashboard > Settings > API > Project API keys > service_role 取得
SUPABASE_SERVICE_ROLE_KEY="your-service-role-key-here"

# ============================================
# 應用程式設定
# ============================================
NODE_ENV="development"  # development, production, test
PORT=3000
API_PREFIX="/api/v1"

# ============================================
# 爬蟲設定
# ============================================
SCRAPER_USER_AGENT="Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36"
SCRAPER_DELAY_MS=1000  # 每次請求之間的延遲 (毫秒)，避免被封鎖
SCRAPER_TIMEOUT_MS=30000  # 請求超時時間 (毫秒)

# ============================================
# OpenAI API 設定 (爬蟲系統必需)
# ============================================
# 從 https://platform.openai.com/api-keys 獲取 API Key
OPENAI_API_KEY="your-openai-api-key-here"

# 可選：指定使用的模型 (預設使用 gpt-4o-mini)
# OPENAI_MODEL="gpt-4o-mini"

# ============================================
# 日誌設定
# ============================================
LOG_LEVEL="info"  # debug, info, warn, error
LOG_FORMAT="json"  # json, pretty

# ============================================
# 快取設定 (可選，未來擴展使用)
# ============================================
# REDIS_URL="redis://localhost:6379"
# CACHE_TTL_SECONDS=3600

# ============================================
# 檔案儲存設定 (可選，用於儲存圖片)
# ============================================
# 如需使用 Supabase Storage
# SUPABASE_STORAGE_BUCKET="product-images"
# 或使用其他雲端儲存服務
# AWS_S3_BUCKET="your-bucket-name"
# AWS_ACCESS_KEY_ID="your-access-key"
# AWS_SECRET_ACCESS_KEY="your-secret-key"
# AWS_REGION="ap-northeast-1"
